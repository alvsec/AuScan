name: CI

on:
  # main se comprueba en cada push, como promete SECURITY.md. Las ramas
  # de trabajo se comprueban vía pull_request, que ya cubre cada commit
  # de una rama con PR abierta sin duplicar la matriz completa por el
  # mismo commit — que es lo que branches-ignore no conseguía.
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - run: npm ci

      - name: Comprobaciones
        run: npm run check

      - name: Clippy
        run: cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings

      - name: Formato de Rust
        run: cargo fmt --manifest-path src-tauri/Cargo.toml -- --check

      - name: Build
        run: npm run build && cargo build --manifest-path src-tauri/Cargo.toml
